import { Clock3, LogOut, ShieldAlert } from "lucide-react"; import { useNavigate } from "react-router-dom"; import { useAuth } from "../context/AuthContext"; import { formatRemaining } from "../utils/auth"; import { Button } from "./ui";
export default function SessionTimeoutModal() { const { session, stayLoggedIn, logout } = useAuth(); const navigate = useNavigate(); if (!session?.showWarning) return null; const exit = () => { logout(); navigate("/login", { replace: true, state: { reason: "expired" } }); }; return <div className="fixed inset-0 z-[100] grid place-items-center bg-ink/70 p-5 backdrop-blur-sm"><div className="w-full max-w-[430px] animate-[modal-in_180ms_ease-out] rounded-3xl border border-white/[0.1] bg-[#102044] p-6 text-white shadow-[0_28px_80px_rgba(4,10,25,.4)] sm:p-8"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-[#f4c96b]/15 text-[#f4c96b]"><ShieldAlert size={22} /></div><h2 className="mt-6 font-display text-2xl font-semibold tracking-[-0.04em]">Your session is ending soon</h2><p className="mt-3 text-sm leading-6 text-slate-400">For your security, you’ll be signed out automatically when the timer reaches zero.</p><div className="mt-6 flex items-center gap-3 rounded-xl border border-white/[0.08] bg-white/[0.04] p-3.5"><Clock3 size={17} className="text-coral" /><span className="text-sm text-slate-300">Time remaining</span><strong className="ml-auto font-mono text-lg text-white">{formatRemaining(session.remainingMs)}</strong></div><div className="mt-7 grid grid-cols-2 gap-3"><Button variant="secondary" onClick={exit}><LogOut size={15} />Log out</Button><Button onClick={stayLoggedIn}>Stay logged in</Button></div></div></div>; }
