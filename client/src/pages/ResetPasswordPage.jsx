import { ArrowLeft, ArrowRight, CheckCircle2 } from "lucide-react"; import { useState } from "react"; import { Link, useNavigate } from "react-router-dom"; import AuthShell from "../components/AuthShell"; import PasswordStrength, { getPasswordScore } from "../components/PasswordStrength"; import { Alert, Button, PasswordInput } from "../components/ui"; import { useAuth } from "../context/AuthContext"; import { validatePassword } from "../utils/auth";
export default function ResetPasswordPage() { const { resetPassword } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ password: "", confirm: "" }); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const [success, setSuccess] = useState(false); const update = (key, value) => setForm((current) => ({ ...current, [key]: value })); const submit = async (event) => { event.preventDefault(); const passwordError = validatePassword(form.password); if (passwordError || getPasswordScore(form.password).score < 5) { setError(passwordError || "Add all five password requirements."); return; } if (form.password !== form.confirm) { setError("Passwords do not match."); return; } setError(""); setLoading(true); try { await resetPassword(form.password); setSuccess(true); window.setTimeout(() => navigate("/login", { replace: true }), 1800); } catch (requestError) { setError(requestError.message); } finally { setLoading(false); } }; return <AuthShell eyebrow="ACCOUNT RECOVERY" title="Choose a new password." subtitle="Create a strong password you’ll remember. This mock reset flow is stored locally in your browser." step="Reset password" footer={<span>Need a new reset link? <Link to="/forgot-password" className="font-semibold text-slate-400 transition hover:text-coral">Start again <ArrowRight size={12} className="inline" /></Link></span>}>{success ? <div className="space-y-5 rounded-2xl border border-mint/20 bg-mint/[0.06] p-5"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-mint/10 text-mint"><CheckCircle2 size={23} /></div><h2 className="font-display text-xl font-semibold text-white">Password updated</h2><p className="text-sm leading-6 text-slate-400">Your new password is saved. Taking you back to sign in…</p></div> : <form onSubmit={submit} className="space-y-5">{error && <Alert>{error}</Alert>}<PasswordInput label="New password" id="reset-password" autoComplete="new-password" value={form.password} onChange={(event) => update("password", event.target.value)} placeholder="Create a secure password" /><PasswordStrength password={form.password} /><PasswordInput label="Confirm new password" id="reset-confirm" autoComplete="new-password" value={form.confirm} onChange={(event) => update("confirm", event.target.value)} placeholder="Repeat your password" /><Button type="submit" loading={loading} className="w-full">Reset password <ArrowRight size={16} /></Button><Link to="/login" className="flex items-center justify-center gap-2 text-xs font-semibold text-slate-500 hover:text-white"><ArrowLeft size={13} />Back to sign in</Link></form>}</AuthShell>; }
