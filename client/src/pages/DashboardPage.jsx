import { Activity, ArrowUpRight, CheckCircle2, Clock3, KeyRound, Lock, MoreHorizontal, ShieldCheck, Sparkles, Users } from "lucide-react";
import { useEffect, useState } from "react";
import { Link, useLocation } from "react-router-dom";
import DashboardLayout from "../components/DashboardLayout";
import { Alert } from "../components/ui";
import { useAuth } from "../context/AuthContext";
import { formatDate, formatDateTime, formatRemaining, getInitials } from "../utils/auth";
const cardTone = { coral: "bg-coral/10 text-coral", sky: "bg-sky/10 text-[#238ec3]", mint: "bg-mint/10 text-[#279965]" };
export default function DashboardPage() { const { currentUser, session, getActivities, dataVersion } = useAuth(); const location = useLocation(); const [notice, setNotice] = useState(() => location.state?.notice || sessionStorage.getItem("saas_login_notice") || ""); const [now, setNow] = useState(() => new Date()); const activities = getActivities(); useEffect(() => { if (notice) { sessionStorage.removeItem("saas_login_notice"); const timer = window.setTimeout(() => setNotice(""), 5000); return () => window.clearTimeout(timer); } }, [notice]); useEffect(() => { const id = window.setInterval(() => setNow(new Date()), 60000); return () => window.clearInterval(id); }, []); const latestActivities = activities.slice(0, 4); const stats = [{ label: "Auth events", value: String(activities.length), change: "Local history", icon: Activity, tone: "coral" }, { label: "Protected sessions", value: "1,284", change: "+18.2%", icon: Users, tone: "sky" }, { label: "Security score", value: currentUser?.isVerified ? "A+" : "B", change: currentUser?.isVerified ? "Excellent" : "Verify email", icon: ShieldCheck, tone: "mint" }]; return <DashboardLayout><div className="mx-auto max-w-[1240px]">{notice && <Alert variant="success" className="mb-5">{notice}</Alert>}<div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-3 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.18em] text-coral"><span className="h-1.5 w-1.5 rounded-full bg-coral" />{formatDate(now)}</div><h1 className="font-display text-3xl font-semibold tracking-[-0.05em] text-ink sm:text-[2.75rem]">Your Northstar, at a glance.</h1><p className="mt-2 text-sm text-slate-500">A calm view of the systems keeping your digital experience secure.</p></div><div className="flex items-center gap-2 rounded-full bg-mint/10 px-3.5 py-2 text-xs font-semibold text-[#279965]"><span className="h-2 w-2 rounded-full bg-mint" />All systems operational</div></div><div className="mt-9 grid gap-4 md:grid-cols-3">{stats.map(({ label, value, change, icon: Icon, tone }) => <div key={label} className="animate-[fade-up_260ms_ease-out] rounded-2xl bg-white p-5 shadow-[0_10px_30px_rgba(10,23,55,0.04)] ring-1 ring-ink/[0.05] transition hover:-translate-y-0.5 hover:shadow-[0_16px_35px_rgba(10,23,55,0.08)]"><div className="flex items-start justify-between"><span className={`grid h-10 w-10 place-items-center rounded-xl ${cardTone[tone]}`}><Icon size={18} /></span><MoreHorizontal size={18} className="text-slate-300" /></div><p className="mt-6 text-xs font-medium text-slate-500">{label}</p><div className="mt-1 flex items-end justify-between"><strong className="font-display text-3xl font-semibold tracking-[-0.05em] text-ink">{value}</strong><span className={`mb-1 text-xs font-semibold ${tone === "coral" ? "text-coral" : tone === "sky" ? "text-[#238ec3]" : "text-[#279965]"}`}>{change}</span></div></div>)}</div><div className="mt-5 grid gap-5 xl:grid-cols-[1.3fr_0.7fr]"><section className="rounded-2xl bg-ink p-6 text-white shadow-[0_18px_40px_rgba(10,23,55,0.11)] sm:p-7"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-slate-500">Security overview</p><h2 className="mt-2 font-display text-2xl font-semibold tracking-[-0.045em]">Your account is protected.</h2><p className="mt-2 max-w-[390px] text-sm leading-6 text-slate-400">Northstar is monitoring your local session and verification state for this demo.</p></div><span className="grid h-11 w-11 place-items-center rounded-2xl bg-mint/10 text-mint"><Lock size={19} /></span></div><div className="mt-8 grid gap-3 sm:grid-cols-3"><div className="rounded-xl border border-white/[0.08] bg-white/[0.04] p-4"><p className="text-[10px] uppercase tracking-[0.15em] text-slate-600">Account status</p><p className="mt-2 flex items-center gap-2 text-sm font-semibold text-mint"><CheckCircle2 size={14} />Active</p></div><div className="rounded-xl border border-white/[0.08] bg-white/[0.04] p-4"><p className="text-[10px] uppercase tracking-[0.15em] text-slate-600">Email verification</p><p className="mt-2 flex items-center gap-2 text-sm font-semibold text-mint"><ShieldCheck size={14} />{currentUser?.isVerified ? "Verified" : "Pending"}</p></div><div className="rounded-xl border border-white/[0.08] bg-white/[0.04] p-4"><p className="text-[10px] uppercase tracking-[0.15em] text-slate-600">Session status</p><p className="mt-2 flex items-center gap-2 font-mono text-sm font-semibold text-white"><Clock3 size={14} className="text-coral" />{formatRemaining(session?.remainingMs || 0)}</p></div></div></section><section className="rounded-2xl bg-white p-6 shadow-[0_10px_30px_rgba(10,23,55,0.04)] ring-1 ring-ink/[0.05] sm:p-7"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-slate-400">Account profile</p><h2 className="mt-2 font-display text-xl font-semibold tracking-[-0.04em] text-ink">{currentUser?.name}</h2></div><span className="grid h-11 w-11 place-items-center rounded-2xl bg-ink text-sm font-bold text-white">{getInitials(currentUser?.name)}</span></div><p className="mt-1 truncate text-xs text-slate-500">{currentUser?.email}</p><div className="mt-7 rounded-xl bg-[#f5f7fb] p-4"><div className="flex items-center gap-3"><Sparkles size={16} className="text-coral" /><p className="text-xs font-semibold text-ink">Complete your profile</p><span className="ml-auto text-xs font-bold text-coral">68%</span></div><div className="mt-3 h-1.5 rounded-full bg-ink/[0.08]"><div className="h-full w-[68%] rounded-full bg-coral" /></div><p className="mt-3 text-xs leading-5 text-slate-500">Add a profile photo and recovery method to finish setup.</p></div><Link to="/profile" className="mt-5 flex items-center gap-2 text-xs font-semibold text-ink transition hover:text-coral focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-coral/40">Manage profile <ArrowUpRight size={14} /></Link></section></div><section className="mt-5 rounded-2xl bg-white p-6 shadow-[0_10px_30px_rgba(10,23,55,0.04)] ring-1 ring-ink/[0.05] sm:p-7"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-slate-400">Activity log</p><h2 className="mt-2 font-display text-xl font-semibold tracking-[-0.04em] text-ink">Recent activity</h2></div><Link to="/activity" className="text-xs font-semibold text-coral">View all</Link></div><div className="mt-6 divide-y divide-ink/[0.06]">{latestActivities.length ? latestActivities.map((event) => <div key={event.id} className="flex items-center gap-4 py-3.5 first:pt-0 last:pb-0"><span className="grid h-9 w-9 place-items-center rounded-xl bg-coral/10 text-coral"><KeyRound size={16} /></span><span className="text-sm font-medium text-ink">{event.title}</span><span className="ml-auto text-xs text-slate-400">{formatDateTime(event.createdAt)}</span></div>) : <p className="py-5 text-sm text-slate-500">No activity yet. Your next action will appear here.</p>}</div></section></div></DashboardLayout>; }
