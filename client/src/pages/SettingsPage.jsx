import { Bell, CheckCircle2, LogOut, Monitor, Moon, Palette, Save, ShieldCheck, Sun } from "lucide-react";
import { useState } from "react";
import { useNavigate } from "react-router-dom";
import DashboardLayout from "../components/DashboardLayout";
import { Alert, Button, Input, PasswordInput } from "../components/ui";
import { useAuth } from "../context/AuthContext";
import { useTheme } from "../context/ThemeContext";
import { formatRemaining, validatePassword } from "../utils/auth";
export default function SettingsPage() { const { currentUser, session, updateProfile, changePassword, logout } = useAuth(); const { theme, resolvedTheme, setTheme } = useTheme(); const navigate = useNavigate(); const [name, setName] = useState(currentUser?.name || ""); const [email, setEmail] = useState(currentUser?.email || ""); const [password, setPassword] = useState(""); const [notifications, setNotifications] = useState(() => localStorage.getItem("northstar_notifications_preference") !== "off"); const [message, setMessage] = useState(""); const [error, setError] = useState(""); const saveAccount = async () => { setError(""); try { await updateProfile({ name, email }); setMessage("Account details saved successfully."); } catch (requestError) { setError(requestError.message); } }; const savePassword = async () => { setError(""); const validation = validatePassword(password); if (validation) { setError(validation); return; } try { await changePassword(password); setPassword(""); setMessage("Password changed successfully."); } catch (requestError) { setError(requestError.message); } }; const setNotificationsPreference = (value) => { setNotifications(value); localStorage.setItem("northstar_notifications_preference", value ? "on" : "off"); setMessage(`Notifications ${value ? "enabled" : "disabled"}.`); }; const signOut = () => { logout(); navigate("/login", { replace: true }); }; return <DashboardLayout><div className="mx-auto max-w-[980px]"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-coral">Workspace preferences</p><h1 className="mt-2 font-display text-3xl font-semibold tracking-[-0.05em] text-ink">Settings</h1><p className="mt-2 text-sm text-slate-500">Control your Northstar account, security, and experience.</p>{(message || error) && <Alert variant={error ? "error" : "success"} className="mt-5">{error || message}</Alert>}<div className="mt-8 grid gap-5"><section className="rounded-2xl bg-white p-6 shadow-[0_10px_30px_rgba(10,23,55,0.04)] ring-1 ring-ink/[0.05] sm:p-7"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-coral/10 text-coral"><Palette size={17} /></span><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-slate-400">Account</p><h2 className="mt-1 font-display text-xl font-semibold text-ink">Account details</h2></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><Input label="Full name" id="settings-name" value={name} onChange={(event) => setName(event.target.value)} /><Input label="Email" id="settings-email" type="email" value={email} onChange={(event) => setEmail(event.target.value)} /></div><Button className="mt-5" onClick={saveAccount}><Save size={15} />Save changes</Button></section><section className="rounded-2xl bg-white p-6 shadow-[0_10px_30px_rgba(10,23,55,0.04)] ring-1 ring-ink/[0.05] sm:p-7"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-mint/10 text-[#279965]"><ShieldCheck size={17} /></span><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-slate-400">Security</p><h2 className="mt-1 font-display text-xl font-semibold text-ink">Security controls</h2></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-xl bg-[#f5f7fb] p-4"><p className="text-xs font-semibold text-ink">Email verification</p><p className="mt-1 flex items-center gap-1.5 text-xs text-[#279965]"><CheckCircle2 size={13} />Verified</p></div><div className="rounded-xl bg-[#f5f7fb] p-4"><p className="text-xs font-semibold text-ink">Session timeout</p><p className="mt-1 text-xs text-slate-500">{formatRemaining(session?.remainingMs || 0)} remaining</p></div></div><div className="mt-5 max-w-[450px] space-y-4"><PasswordInput label="New password" id="settings-password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Create a new password" /><Button onClick={savePassword}>Change Password</Button></div></section><section className="rounded-2xl bg-white p-6 shadow-[0_10px_30px_rgba(10,23,55,0.04)] ring-1 ring-ink/[0.05] sm:p-7"><div className="grid gap-7 md:grid-cols-2"><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-slate-400">Preferences</p><h2 className="mt-1 font-display text-xl font-semibold text-ink">Theme</h2><p className="mt-2 text-sm text-slate-500">Choose how Northstar should look. Current display: <strong className="text-ink">{resolvedTheme}</strong>.</p><div className="mt-4 grid grid-cols-3 gap-2">{[{ value: "light", label: "Light", icon: Sun }, { value: "dark", label: "Dark", icon: Moon }, { value: "system", label: "System", icon: Monitor }].map(({ value, label, icon: Icon }) => <button key={value} onClick={() => setTheme(value)} className={`flex flex-col items-center gap-2 rounded-xl border p-3 text-xs font-semibold transition ${theme === value ? "border-coral bg-coral/10 text-coral" : "border-ink/[0.08] text-slate-500 hover:border-ink/20"}`}><Icon size={16} />{label}</button>)}</div></div><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-slate-400">Notifications</p><h2 className="mt-1 font-display text-xl font-semibold text-ink">Security updates</h2><p className="mt-2 text-sm text-slate-500">Show local alerts for sign-ins and account changes.</p><button onClick={() => setNotificationsPreference(!notifications)} className={`mt-5 flex w-full items-center justify-between rounded-xl p-4 text-left transition ${notifications ? "bg-mint/10" : "bg-ink/[0.05]"}`}><span className="flex items-center gap-3"><Bell size={17} className={notifications ? "text-[#279965]" : "text-slate-400"} /><span className="text-sm font-semibold text-ink">Notifications</span></span><span className={`relative h-6 w-11 rounded-full transition ${notifications ? "bg-mint" : "bg-slate-300"}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white shadow transition ${notifications ? "left-6" : "left-1"}`} /></span></button></div></div></section><section className="rounded-2xl bg-ink p-6 text-white shadow-[0_18px_40px_rgba(10,23,55,0.11)] sm:p-7"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-center"><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-slate-500">Session</p><h2 className="mt-2 font-display text-xl font-semibold">Your session is active.</h2><p className="mt-2 text-sm text-slate-400">Log out when you’re finished using this shared demo environment.</p></div><Button variant="secondary" onClick={signOut}><LogOut size={15} />Logout</Button></div></section></div></div></DashboardLayout>; }
